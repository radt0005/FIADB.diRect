% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GB_est_w_filter.R
\name{GB_est_w_filter}
\alias{GB_est_w_filter}
\title{Calculate FIA Green Book Estimates with Filters}
\usage{
GB_est_w_filter(
  EVAL_GRP,
  ATTRIBUTE_NBR,
  GRP_BY_ATTRIB = "STATECD",
  SCHEMA = "FS_FIADB",
  VAR_NAMES = NA,
  VAR_VALUES = NA,
  VAR_CONDS = NA,
  VAR_BOOLS = NA,
  dbname = "fiadb"
)
}
\arguments{
\item{EVAL_GRP}{FIA evaluation group identifier. Integer of the form
SSYYYY, where SS is the two-digit state FIPS code and YYYY is the
evaluation year.}

\item{ATTRIBUTE_NBR}{FIA attribute number.}

\item{GRP_BY_ATTRIB}{Character vector of FIA variables used to group
the estimates.}

\item{SCHEMA}{PostgreSQL schema containing the FIA tables.}

\item{VAR_NAMES}{Character vector of FIA variable names used in the
filter specification.}

\item{VAR_VALUES}{Values associated with \code{VAR_NAMES}.}

\item{VAR_CONDS}{Character vector of comparison operators (for
example, \code{"="}, \code{">"}, or \code{"IN"}).}

\item{VAR_BOOLS}{Optional logical operators used to combine filter
conditions (for example, \code{"AND"} or \code{"OR"}).}

\item{dbname}{PostgreSQL database name.}
}
\value{
A data frame containing FIA Green Book estimates and associated
summary statistics.
}
\description{
Generates FIA Green Book estimates for a specified evaluation group
and attribute using filter criteria supplied as variable names,
values, comparison operators, and logical connectors.
}
\details{
This function simplifies filtered analyses by constructing the
required SQL filter expression and passing it to \code{GB_est()}.
}
\examples{
\dontrun{
GB_est_w_filter(
  EVAL_GRP = 102019,
  ATTRIBUTE_NBR = 2,
  GRP_BY_ATTRIB = "STATECD",
  VAR_NAMES = "STATUSCD",
  VAR_VALUES = 1,
  VAR_CONDS = "="
)
}

}
